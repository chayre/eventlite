<p>
  <% if user.email == current_user.email %>
    <strong>Name:</strong>
    <%= user.name %>
  <% end %>
</p>
<br>

<p>
  <strong>Username:</strong>
  <%= user.username %>
</p>
<br>

<p>
  <% if user.email == current_user.email %>
    <strong>Email:</strong>
    <%= user.email %>
  <% end %>
</p>
<br>

<p>
  <% if @user.events.any? %>
    <strong>Hosted <%= @user.events.count %> events</strong>
  <% else %>
    <strong>Hosted <%= 0 %> events</strong>
  <% end %>
</p><br>

<% if user.email == current_user.email  %>
    <p class="control">
        <%= link_to "Edit or delete account" , edit_user_registration_path, class: "button is-info" %>
    </p>
<% end %>