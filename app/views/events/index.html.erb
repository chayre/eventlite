<p style="color: green"><%= notice %></p>

<section class="section">
  <div class="container">
    <div class="columns">
   <div id="events">
  <% @events.each do |event| %>
    <%= render event %>
    <p class="show">
      <%= link_to "Show this event", event %>
    </p>
  <% end %>
</div>
</div>
</div>
</section>